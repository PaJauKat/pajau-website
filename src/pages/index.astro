---
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="description"
      content="Pajau es un alias personal y un portafolio de proyectos, herramientas y trabajo creativo."
    />
    <title>Pajau</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="backdrop"></div>

    <div class="page-shell">
      <header class="topbar">
        <div class="brand-wrap">
          <img src="/logo.png" alt="Logo de Pajau" class="brand-logo" />
          <span class="brand-name">Pajau</span>
        </div>

        <nav class="nav" aria-label="Navegación principal">
          <a href="#inicio">Inicio</a>
          <a href="#proyectos">Proyectos</a>
          <a href="https://github.com/PaJauKat" target="_blank" rel="noreferrer">GitHub</a>
        </nav>
      </header>

      <main id="inicio" class="hero">
        <div class="hero-copy">
          <p class="eyebrow">Personal brand</p>
          <h1>Crear cosas con criterio. Cuidar cada detalle.</h1>
          <p class="subtitle">
            Pajau es mi alias personal: un espacio para proyectos, herramientas y ideas que valen la pena construir bien.
          </p>

          <div class="actions">
            <a href="/plugins" class="button primary">Ver proyectos</a>
            <a href="/ca-tier-calculator" class="button secondary">Calculadora CA</a>
            <a href="https://github.com/PaJauKat" class="button ghost" target="_blank" rel="noreferrer">GitHub</a>
          </div>
        </div>

        <div class="hero-visual">
          <div class="image-frame">
            <img src="/katarina2.jpg" alt="Imagen estética inspirada en Katarina" />
          </div>
        </div>
      </main>

      <section id="proyectos" class="projects">
        <div class="section-header">
          <p class="section-kicker">Proyectos</p>
          <h2>Un portfolio de trabajo con identidad.</h2>
        </div>

        <div class="project-grid">
          <a href="/ca-tier-calculator" class="project-card project-card--featured">
            <div class="card-topline">
              <span class="card-tag">Herramienta</span>
              <span class="card-arrow">↗</span>
            </div>
            <h3>Calculadora CA</h3>
            <p>Proyecto práctico para ordenar, comparar y visualizar ideas de forma clara y útil.</p>
          </a>

          <a href="/plugins" class="project-card">
            <div class="card-topline">
              <span class="card-tag">Utilidades</span>
              <span class="card-arrow">↗</span>
            </div>
            <h3>Plugins</h3>
            <p>Pequeños recursos pensados para mejorar la experiencia y simplificar tareas.</p>
          </a>

          <article class="project-card">
            <div class="card-topline">
              <span class="card-tag">Futuro</span>
              <span class="card-arrow">→</span>
            </div>
            <h3>Más proyectos</h3>
            <p>La base está diseñada para crecer con nuevos lanzamientos sin saturar la navegación.</p>
          </article>
        </div>
      </section>
    </div>
  </body>
</html>

<style>
  :root {
    --bg: #090c10;
    --bg-2: #101821;
    --panel: rgba(16, 20, 27, 0.82);
    --panel-soft: rgba(19, 26, 33, 0.8);
    --line: rgba(255, 255, 255, 0.08);
    --text: #edf1f5;
    --muted: #a2acba;
    --soft: #dfe7f3;
    --ghost: rgba(255, 255, 255, 0.02);
    --shadow: 0 28px 50px rgba(0, 0, 0, 0.28);
  }

  * {
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    margin: 0;
    min-height: 100vh;
    background:
      radial-gradient(circle at top, rgba(255, 255, 255, 0.06), transparent 28%),
      linear-gradient(180deg, #090d11 0%, #0b1015 100%);
    color: var(--text);
    font-family: "Inter", sans-serif;
    display: flex;
    justify-content: center;
    padding: 26px 18px 48px;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  img {
    max-width: 100%;
    display: block;
  }

  .backdrop {
    position: fixed;
    inset: 0;
    pointer-events: none;
    background: radial-gradient(circle at center, rgba(255, 255, 255, 0.04), transparent 48%);
    filter: blur(50px);
  }

  .page-shell {
    position: relative;
    width: min(1180px, 100%);
    z-index: 1;
  }

  .topbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 18px;
    min-height: 72px;
    border-bottom: 1px solid var(--line);
    padding-bottom: 18px;
  }

  .brand-wrap {
    display: inline-flex;
    align-items: center;
    gap: 12px;
  }

  .brand-logo {
    width: 34px;
    height: 34px;
    border-radius: 10px;
    object-fit: cover;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--line);
  }

  .brand-name {
    font-weight: 700;
    letter-spacing: -0.04em;
    font-size: 1.1rem;
  }

  .nav {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 16px;
    color: var(--muted);
    font-size: 0.86rem;
    letter-spacing: 0.02em;
  }

  .nav a {
    transition: color 0.2s ease, opacity 0.2s ease;
    opacity: 0.9;
  }

  .nav a:hover {
    color: var(--text);
    opacity: 1;
  }

  .hero {
    display: grid;
    grid-template-columns: 1.25fr 0.75fr;
    gap: 42px;
    align-items: center;
    padding: 72px 0 42px;
  }

  .eyebrow,
  .section-kicker {
    margin: 0 0 18px;
    text-transform: uppercase;
    letter-spacing: 0.13em;
    font-size: 0.7rem;
    color: var(--muted);
  }

  h1 {
    margin: 0;
    font-size: clamp(2.8rem, 5vw, 5.3rem);
    line-height: 0.94;
    letter-spacing: -0.07em;
    font-weight: 700;
    max-width: 700px;
  }

  .subtitle {
    margin-top: 22px;
    max-width: 620px;
    color: var(--muted);
    font-size: 1.08rem;
    line-height: 1.8;
  }

  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: 32px;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 46px;
    padding: 0 18px;
    border-radius: 999px;
    border: 1px solid var(--line);
    font-weight: 600;
    transition: transform 0.2s ease, border-color 0.2s ease, background 0.2s ease;
  }

  .button:hover {
    transform: translateY(-1px);
    border-color: rgba(255, 255, 255, 0.18);
  }

  .button.primary {
    background: linear-gradient(180deg, #f6f9fd 0%, #dde5ee 100%);
    color: #0b1015;
    box-shadow: 0 20px 30px rgba(255, 255, 255, 0.08);
  }

  .button.secondary {
    background: rgba(255, 255, 255, 0.02);
    color: var(--soft);
  }

  .button.ghost {
    background: rgba(255, 255, 255, 0.02);
    color: var(--soft);
  }

  .hero-visual {
    display: flex;
    justify-content: center;
  }

  .image-frame {
    position: relative;
    width: min(100%, 420px);
    border-radius: 28px;
    overflow: hidden;
    border: 1px solid var(--line);
    background: var(--panel-soft);
    box-shadow: var(--shadow);
  }

  .image-frame::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(0, 0, 0, 0.1), rgba(0, 0, 0, 0.2));
    z-index: 1;
  }

  .image-frame img {
    width: 100%;
    height: 560px;
    object-fit: cover;
    object-position: center;
  }

  .projects {
    padding-top: 10px;
  }

  .section-header {
    margin-bottom: 22px;
  }

  .section-header h2 {
    margin: 0;
    font-size: clamp(1.9rem, 3vw, 2.8rem);
    letter-spacing: -0.05em;
  }

  .project-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
  }

  .project-card {
    display: block;
    padding: 22px 20px 20px;
    background: linear-gradient(180deg, rgba(20, 25, 33, 0.88), rgba(9, 12, 17, 0.9));
    border: 1px solid var(--line);
    border-radius: 24px;
    box-shadow: var(--shadow);
    transition: transform 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .project-card:hover {
    transform: translateY(-3px);
    border-color: rgba(255, 255, 255, 0.16);
    box-shadow: 0 26px 42px rgba(0, 0, 0, 0.32);
  }

  .project-card--featured {
    background: linear-gradient(180deg, rgba(29, 36, 46, 0.94), rgba(11, 14, 19, 0.92));
  }

  .card-topline {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 18px;
  }

  .card-tag {
    display: inline-block;
    color: var(--muted);
    font-size: 0.68rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .card-arrow {
    color: var(--soft);
    font-size: 1.05rem;
    opacity: 0.9;
  }

  .project-card h3 {
    margin: 0 0 10px;
    font-size: 1.3rem;
    letter-spacing: -0.04em;
  }

  .project-card p {
    margin: 0;
    color: var(--muted);
    line-height: 1.7;
  }

  @media (max-width: 860px) {
    .hero {
      grid-template-columns: 1fr;
    }

    .project-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 560px) {
    .topbar {
      flex-direction: column;
      align-items: flex-start;
      padding-bottom: 14px;
    }

    .nav {
      gap: 10px 18px;
    }

    .image-frame img {
      height: 440px;
    }
  }
</style>